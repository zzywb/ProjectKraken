var width = canvas.width;
var height = canvas.height;
var show_fps = true;

var keyMap = {
  fire1: 'Enter',
  up: ['ArrowUp', 'w'],
  down: ['ArrowDown', 's'],
  left: ['ArrowLeft', 'a'],
  right: ['ArrowRight', 'd']
};